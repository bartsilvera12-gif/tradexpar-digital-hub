import { test } from "@playwright/test";

// Plantilla: duplicá este archivo, quitá `.skip` y añadí aserciones reales.
test.skip("ejemplo E2E (pendiente)", async () => {
  // await page.goto("/");
});
