# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# `dist` versionado para despliegue estático (build: npm run build).
dist-ssr
*.local

# Artefacto local para Hostinger (npm run zip:dist); no subir el ZIP al repo.
tradexpar-dist.zip

# Variables de entorno (secretos); usá `.env.example` como plantilla.
.env
.env.local
.env.*.local
.env.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright (artefactos locales)
test-results/
playwright-report/

# Cache imágenes Fastrax en el servidor de pagos (solo .gitkeep versionado)
server/public/fastrax-products/*
!server/public/fastrax-products/.gitkeep
