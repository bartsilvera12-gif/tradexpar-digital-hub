# Ejemplo: mismo dominio para la tienda (estáticos) y rutas /api → Node (server de pagos).
# Ajustá server_name, rutas root y el upstream (puerto PM2 / Docker).
#
# Comprobación rápida tras recargar Nginx:
#   curl -sS https://TU_DOMINIO/health
#   curl -sS -o /dev/null -w "%{http_code}" -H "x-api-key: TU_API_PUBLIC_KEY" \
#     -X POST https://TU_DOMINIO/api/public/orders/UUID-PRUEBA/create-payment -H "Content-Type: application/json" -d "{}"
#
# server {
#   listen 443 ssl http2;
#   server_name tienda.ejemplo.com;
#
#   # ssl_certificate ...;
#   # ssl_certificate_key ...;
#
#   root /var/www/tradexpar/dist;
#   index index.html;
#
#   location /api/ {
#     proxy_pass http://127.0.0.1:8787;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
#
#   location /health {
#     proxy_pass http://127.0.0.1:8787/health;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
#
#   location / {
#     try_files $uri $uri/ /index.html;
#   }
# }
